{
  "build": {
    "command": "pip install -r requirements.txt && python -c 'import nltk; nltk.download(\"punkt\"); nltk.download(\"stopwords\")'"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "gunicorn web_app:app"
  }
}